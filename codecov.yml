codecov:
  branch: master

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto

comment:
  layout: "reach,diff,flags,files"
  behavior: default
